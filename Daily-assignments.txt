19-Feb
--------
function saveUser() {
    const user = {
        name: "John",
        age: 30
    };
    localStorage.setItem("user", JSON.stringify(user));
    const savedUser = localStorage.getItem("user");
    console.log(typeof savedUser);
    const parsedUser = JSON.parse(savedUser);
    console.log(parsedUser);
}
saveUser();


var a = 10;
a = "Hello";
console.log(typeof a);

const add = function () {
    console.log(arguments);
    let sum = 0;
    for (let i = 0; i < arguments.length; i++) {
        // console.log(arguments[i]);
        sum += arguments[i];
    }
    return sum;
}
const result = add(5, 10, 23, 34, 45);
console.log(result);

18-Feb
---------

  <build>
    <pluginManagement><!-- lock down plugins versions to avoid using Maven defaults (may be moved to parent pom) -->
      <plugins>
        <!-- clean lifecycle, see https://maven.apache.org/ref/current/maven-core/lifecycles.html#clean_Lifecycle -->
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <version>3.4.0</version>
        </plugin>
        <!-- default lifecycle, jar packaging: see https://maven.apache.org/ref/current/maven-core/default-bindings.html#Plugin_bindings_for_jar_packaging -->
        <plugin>
          <artifactId>maven-resources-plugin</artifactId>
          <version>3.3.1</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.3.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>3.1.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>3.1.2</version>
        </plugin>
        <!-- site lifecycle, see https://maven.apache.org/ref/current/maven-core/lifecycles.html#site_Lifecycle -->
        <plugin>
          <artifactId>maven-site-plugin</artifactId>
          <version>3.12.1</version>
        </plugin>
        <plugin>
          <artifactId>maven-project-info-reports-plugin</artifactId>
          <version>3.6.1</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
Unit testing
--------------

<dependencies>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>5.10.0</version>
			<scope>test</scope>
		</dependency>

	</dependencies>
----------------------------------------
public class Calculator {

    public int add(int a, int b) {
        return a + b;
    }

    public int divide(int a, int b) {
        return a / b;
    }
}
---------------------------------------------------
class CalculatorTest {

    @Test
    void testAdd() {
        Calculator calc = new Calculator(); //Arrange
        int result = calc.add(2, 3); //act

        assertEquals(4, result);//assert
    }
}

shared files: https://drive.google.com/drive/folders/1LMTK-VknMTI_TkV_UzEttqCdwit2USFW
----------------

9th-Feb
------------
Assignment-1 (Fork-join)
replace numbers with Account objects with random initial balance. 
Use a loop for this. Show the total of balance using Fork-join.
//assignment-2 (Lambda)
1. Define a Lambda function which accepts an array of numbers and returns the same
array after incrementing all numbers by 2.
2. Define a Lambda function which accepts an array of numbers and returns the same
array after incrementing all even numbers by 2.
3. Define a Lambda function which accepts two parameters a)an array of numbers and 
b) incrementValue(say x)
It returns the incremented  array after incrementing all even numbers by x. Implement it with Custom Functional interface.
4. Define a Lambda function which accepts an array of numbers and returns the sum of every third number in the array.
 .
